# MANDATORY SYSTEM RULE: ALWAYS READ GUARDRAILS.MD

> **AUTOMATIC INSTRUCTION**: Any AI agent operating in this workspace MUST automatically perform the following steps before making any code modifications, now or in the future:

1. **Read Guardrails First**: Call `view_file` on `c:\Users\Spartan\Documents\Pranav\Youtoopreneur\js-catering-antigravity-html-css\GUARDRAILS.md` to load all project contracts, section scoping rules, and user-defined constraints.
2. **Strict Section Scoping**: All CSS tweaks must be scoped strictly to specific section IDs (`#act-1`, `#act-3`, etc.).
3. **Automated Verification**: Always execute `node scratch/check_site.js` and `node -c script.js` before completing a turn to guarantee zero syntax errors and zero broken links.
